export default defineAppConfig({
    ui: {
        primary: 'black',
        landing: {
            hero: {
                wrapper: 'min-h-[calc(100vh-var(--header-height))]'
            }
        }
    }
})